/*
Write an SQL statement to query all data that is currently in our birds table.
*/

SELECT * FROM birds;

/* 
We can also specify the columns we want to select:
SELECT id, name, age, species FROM birds;
*/